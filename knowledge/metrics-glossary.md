# Metrics glossary

TODO: for each metric tracked (see metrics/<name>/contract.yaml), one or two
sentences on what it means and why it was chosen. Keep this in sync whenever
a metric contract is added or changed.

- `conversion_rate`: TODO
- `revenue`: TODO
